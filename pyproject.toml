[build-system]
requires = ["setuptools >= 64"]
build-backend = "setuptools.build_meta"

[tool.ruff.lint]
select = ["F", "E", "W"]
ignore = ["E501"]

per-file-ignores = { "**/__init__.py" = ["F401"] }

